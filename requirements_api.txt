# Dependencias SO da api_medicao.py (nao do pipeline inteiro) - usado pelo
# Dockerfile.api_medicao no deploy via Coolify (ver CLAUDE.md, item 18/21).
flask
flask-cors
requests
msgpack
numpy
scipy
gunicorn
boto3
# opencv (cv2) - fallback epipolar (recorte pinhole + NCC) e download de imagem.
# headless = sem deps de GUI, ideal pra servidor.
opencv-python-headless
